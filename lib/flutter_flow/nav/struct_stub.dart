/// Web stub for `dart:ffi`'s Struct, which does not exist on the web.
/// Serialization only ever checks `param is Struct`, which is always
/// false here since FFI types cannot exist on web.
class Struct {
  const Struct();
}
