const page=document.body.dataset.page;const go=p=>location.href=p+'.html';document.addEventListener('click',e=>{const b=e.target.closest('[data-go],[data-upload],[data-scan]');if(!b)return;if(b.dataset.go)go(b.dataset.go);if(b.dataset.upload){const i=document.createElement('input');i.type='file';i.accept='image/*,.pdf';i.onchange=()=>{if(i.files[0]){b.textContent='✓ '+i.files[0].name;b.classList.add('success')}};i.click()}if(b.dataset.scan){b.textContent='Checking camera…';setTimeout(()=>{b.textContent='✓ Liveness check complete';b.classList.add('success')},1000)}});
