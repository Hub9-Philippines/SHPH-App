## 1. Audit & Setup

- [x] 1.1 Run a full lift of inline display strings across `lib/` (excluding `lib/l10n`, `lib/flutter_flow`, `lib/custom_code`, generated code) and triage each into localize / exclude using the D3 rules (exclude: wire values, enum/catalogue codes, storage keys, currency symbols, regex/format templates, debug-only text)
- [x] 1.2 Record the exclude list (non-displayed literals intentionally left hardcoded) so a later audit can confirm nothing display-facing was skipped
- [x] 1.3 Confirm `flutter gen-l10n` currently regenerates cleanly and `flutter analyze` is at the baseline (no new errors) before starting edits

## 2. Base ARB + Foundation

- [ ] 2.1 Add the new screen-prefixed key groups to `app_en.arb` (English) and matching keys to `app_fil.arb` (Filipino) as each batch is drafted, keeping the two files at exact key parity
- [ ] 2.2 Run `flutter gen-l10n` after each ARB edit and fix any placeholder/metadata parity errors before continuing

## 3. Localize by Screen (each batch = draft Tagalog -> review -> finalize)

- [x] 3.1 Localize `booking_funnel` (largest set, ~125 strings): replace literals with `AppLocalizations` lookups, add keys to both ARBs, present the Filipino batch for user review, apply corrections, regenerate + analyze
- [x] 3.2 Localize `tm_flow` (~56 strings) with the same draft -> review -> finalize loop
- [x] 3.3 Localize `address_form` (~32 strings)
- [x] 3.4 Localize `booking_details` (~29 strings)
- [x] 3.5 Localize `security_settings` (~22 strings)
- [x] 3.6 Localize `signin` (~20 strings)
- [x] 3.7 Localize `search_page` (~18 strings)
- [x] 3.8 Localize `settings` + `theme_settings` + `language_settings` + `notification_preferences` (~39 strings combined). All 4 widgets wired (`se*`/`th*`/`lg*`/`np*`); user approved with edits; `gen-l10n` clean; verify with `flutter analyze` 0 errors
- [x] 3.9 Localize `booking_payment` + `booking_success` + `booking` (57 keys combined). All 3 widgets wired (`bp*`/`bs*`/`bk*`); user approved with edits; `gen-l10n` clean; verify with `flutter analyze` 0 errors
- [x] 3.10 Localize `product_page` + `contact_provider` (~27 strings). All pp*/cp* keys added to both ARBs; user approved CSV with edits; product_page_widget.dart and contact_provider_widget.dart fully wired; `gen-l10n` clean; `analyze` 0 errors (699 baseline)
- [x] 3.11 Localize `edit_profile` + `create_profile` + `signup` + `set_password` + `forgot_password` + `otp_page` + `phone_verify_user` (~45 strings). `create_profile` was already fully wired; the other 6 widgets+`otp_page_model` now use `AppLocalizations` (`ep*`/`su*`/`spw*`/`fp*`/`otp*`/`phv*` keys in both ARBs; shared `si*` keys reused for Sign In/Sign Up links; otp model errors threaded via `AppLocalizations` param per D2); `gen-l10n` clean; `analyze` 0 errors (699 baseline)
- [x] 3.12 Localize `sessions` + `biometric_setup` + `my_notifications` + `call_permission` + `call_history_details_page` (~33 strings). All 5 widgets wired; fixed pre-existing bugs: 2 `const` misuse errors (biometric_setup SnackBar, sessions AlertDialog), missing ARB key `bioSetupDeviceNameHint`→used existing `bioSetupDeviceNameLabel`, and 4 stray `,,` syntax errors in my_notifications; added approved `mnErrorMarkRead`/`mnErrorNoDestination`; `gen-l10n` clean; `analyze` 0 errors (701 baseline)
- [x] 3.13 Localize `addresses` + `geographic_selection` + `pin_location` (27 user-approved keys + 3 pl*/4 ge titles). address_widget.dart fully wired (all ad* incl. SnackBars, delete dialog, error details); geographic_selection_widget.dart wired (ge* + `_getTitle` added, `getTitle` model method left in place); pin_location_widget.dart wired (pl*); fixed pre-existing `const` misuse on 3 PopupMenuItems containing `_l10n`; `gen-l10n` clean; `analyze` 0 errors
- [x] 3.14 Localize `room_create` + `room_detail` + `room_join` + `room_list` + `chat_page` (50 user-approved keys: rc*/rd*/rl*/rj*/ck* + shared rl status). All 5 widgets wired; room_join `lookup()` model method threads an `AppLocalizations` param (D2) so the room-not-found error is localized; `_statusLabel` in room_list/room_detail and chat_page all map to `_l10n`; `rdJoined`/`rlSeats`/`rjSeats` use placeholder methods; initState `lookup` moved into `addPostFrameCallback` (initState trap); `gen-l10n` clean; `analyze` 0 errors
- [x] 3.15 Localize `project_create` + `project_detail` + `project_list` + `on_demand_booking` + `dispatch` + `client_ondemand_jobs` (57 user-approved keys: pc*/pd*/pl*/pjStatus*/ob*/cj*). All 5 UI widgets wired (`dispatch` has no dedicated widget — its UI lives in tm_flow done in 3.2, and `dispatch_repository.dart` holds only logging/internal/backend-stored strings, D3-excluded); category dropdown names left as English loanwords (backend ID parity per user); `pdBudget`/`pdHeadcount`/`pdExpires`/`pdScore`/`cjFee`/`cjBids` use placeholder methods; `ob` urgency options localized; on_demand fee row labels localized (₱ amounts/time-slot ranges excluded); de-const'd `ScreenHeader`; `gen-l10n` clean; `analyze` 0 errors
- [x] 3.16 Localize `terms_of_service` + `privacy_policy` + `help` + `report_problem` + `disputes` + `reviews` + `my_reviews` + `write_review` + `recommendations` (105 user-approved keys: tos*/priv*/hp*/cb*/rp*/dp*/rv*/mr*/wr*/rcm*). All 9 UI widgets wired (help = `help_page.dart`+`chatbot_page.dart`). Per user scope: TOU/Privacy body paragraphs + Help FAQ Q/A + chatbot bot greeting/error paragraphs kept English (content, D3 in spirit); localized titles/section headings/buttons/empty/error states/status badges + time-ago labels (user explicitly opted to localize time-ago). `write_review` model threads `AppLocalizations` (D2); `report_problem` `_rpCategoryLabel` maps raw category IDs; `rv`/`mr` use localized time-ago (`{x}m ago` Taglish); time-ago formats kept compact (`{n}h ago`) per user, differing from earlier `ss`/`pp`/`mn` which used `ang nakalipas`; new orphan `privacyPolicy`/`retry` keys pre-existed unused (not reused). `gen-l10n` clean; `analyze` 0 errors
- [x] 3.17 Localize `eta_tracking` + `provider_profile` + `favorites` + `subcategory` + `category_detail` + `categories` + `ai_booking_composer` + `splash` + `not_found` + `onboarding` + `kyc` + `wallet` (62 user-approved keys: et*/ppf*/fav*/subcat*/catd*/catg*/abc*/spl*/nf*/onb*/kycCamera/kycGallery/wl*, appended after rcmBrowse). All 12 targets wired including shared `components/categories_widget/categories_widget.dart` (catgError/catgNoCategories/catgInstantDispatch/catgOpenServices/catg247); category grid names left as English loanwords (backend ID parity). Per user scope: onboarding slide copy kept English (only onbSkip/onbNext localized); AI composer extracted-detail field labels (Service/Description/Budget/Date/Time/Location) kept English (only abc* chrome localized). `et*` unaffected by pre-existing `eta` key; `onb*` used (ob* taken by on_demand_booking); kyc extended existing family with kycCamera/kycGallery. `kyc_document` de-const'd ListTile titles. `gen-l10n` clean; `analyze` 0 errors
- [x] 3.18 Run a full audit of `lib/pages/**/*_model.dart` for model/service toasts lacking a threaded `AppLocalizations` (D2). Result: page-model portion already complete — every page model that sets a user-facing message threads `AppLocalizations` (`WriteReviewModel.submitReview`, `OtpPageModel.sendCode`/`verify`, `RoomJoinModel.lookup`). Remaining page-model literals are dead code (`GeographicSelectionModel.getTitle`, never called — widget uses its own `_getTitle` with `ge*` keys) or D3-excluded format templates (`ChatPageModel._formatTime` `Just now`/`m ago`/`h ago`). Substantive remaining toasts (`ErrorHandler.showError/showSuccess/showConfirmDialog/describeError`, `auth_util`, `post_auth_navigation_flow`, `TMFlowController`) live in `lib/services/` + `lib/auth/` and are explicitly scoped to Task 4.3. Per user: 3.18 marked complete and left at that; service/auth threading deferred to 4.3

## 4. Shared Components & Tabs, Services, Auth, Helpers

- [x] 4.1 Localize shared components in `lib/components/` (67 user-approved `cc*` keys appended to both ARBs after `wlSeeAll`). All display-bearing components wired: auth_prompt_modal, booking_action_row, booking_step_indicator, call_accept_permission_sheet, connectivity_banner, error_state, hero_offer_banner, incoming_job_modal, instant_dispatch_section, invite_earn_banner, invoice_line_items, onboarding_overlay, provider_map_view, provider_proximity_card, provider_status_toggle, quick_book_bar, rich_chat_bar, searchable_select, section_header, step_up_password_modal, waiting_for_client_modal, cupertino_ui/app_feedback, cupertino_ui/app_pickers. `availability_calendar` month/weekday names converted from hardcoded English arrays to `DateFormat` using the app locale (en/fil), with `initializeDateFormatting('en'|'fil')` added in `main()` — no ARB keys for dates per user decision. Structural-only components (booking_card, service_card, empty_state, screen_header, status_pill) verified free of hardcoded strings. `static const` trap in instant_dispatch_section resolved by restructuring `_windows` + `windowLabels`. Dead-code `ProviderMapView` (never instantiated) still localized. `gen-l10n` clean; `analyze` 0 errors (717 baseline)
- [x] 4.2 Localize bottom-tab and provider-dashboard screens in `lib/main/` (~166 keys: sv*/pf*/pm*/pew*/pca*/ms*/hm*/ex*/ctg*/bkf* appended to both ARBs after `ccDismiss`). All 10 wired: services (restructured `_filterLabels` static-const trap into instance getter + sort row `_buildSortRow` de-consted, labels localized while tuple/backend IDs kept), profile (all pf* + reused se*/logOut*/adDefault among tiles/dialogs; `_EditPill` build-local `_l10n`), payment_methods (pm* + shared pmSetDefaultFull/adDefault/retry/cancel; pmEndingIn/pmExpires placeholders), add_ewallet_payment (pew* + pmSetDefaultFull + pmNotAuthenticated SnackBar), add_card_payment (pca* incl. pcaRequired/pcaInvalidMonth/pcaInvalidYear validators + pmSetDefaultFull), messages (ms* incl. segmented segment labels de-consted; `_formatTime` localized msMinutesAgo/msHoursAgo/msDaysAgo/msJustNow; backend call.status/type excluded), home (hm* + category chips display labels hmCatCleaning/Plumbing/Electrical/Painting localized while route/extra backend IDs kept English; hmExit dialog; `_LiveProgressShortcut` build-local `_l10n`; `_HomeLocationSheet` build-local `_l10n`), explore (ex* incl. SectionHeader titles/seeAllLabels de-consted; exServiceProviderFallback ×2), category (ctg*), bookings (bkf* + chips map values localized, enum keys kept; reused exSearchPlaceholder/ccSomethingWrong/retry/spAll). Part A (Tagalog search) code-complete via `lib/utils/tagalog_service_keywords.dart` wired into services_model search filter + search_page matchesQuery/broad-fetch. `gen-l10n` clean; `analyze` 0 errors (728 info baseline)
- [x] 4.3 Localize service-layer/user-facing messages in `lib/services/` and `lib/auth/` (~18 keys: eh*/au*/pan*/pmt*/wt* appended to both ARBs; reused retry/ccOK/ccConfirm/ccCancel/bpPaymentFailed/pfProviderDetected). All 5 targets wired: `error_handler.dart` (threaded nullable `AppLocalizations? l10n` into showError/showErrorDialog/showConfirmDialog, describeError/_getErrorMessage/handleException; removed `connectionErrorMessage` const; generic localized messages replace raw exception text per user; dormant `_getErrorMessage` also localized; updated 5 describeError consumers in signin/signup/phone_verify to pass `_l10n`), `auth_util.dart` (signOutUser/verifyCurrentUserEmail thread optional `l10n`, generic auSignOutError/auVerificationError drop raw `$e`; no live callers — kept localizable), `post_auth_navigation_flow.dart` (provider-notice dialog: pfProviderDetected + panProviderAppBody/panSignOut/panContinueAsClient; nav-error snackbar panNavigationError; de-const'd dialog Texts), `payment_controller.dart` (threaded required `AppLocalizations l10n` into processStripePayment/processMayaPayment; pmtFailedObtainClientSecret/pmtFailedObtainCheckoutUrl/pmtPaymentFailed; generic catch drops `e.toString()`; Stripe `localizedMessage` (SDK) + `'Serbisyo'` brand kept; caller booking_payment_widget passes `_l10n` — audit's live_matching line 748 was a Google Maps route error, not PaymentResult, excluded), `wallet_service.dart` (threaded l10n + `locale` into getWalletData/_txFromBooking/_formatDate; wtUnknownService/wtClient/wtDebitDesc/wtCreditDesc/wtDateNa; dates switched to intl `DateFormat('MMM d, y', locale)` using initializeDateFormatting from Task 4.1; caller wallet_model.loadTransactions threads l10n+locale, wallet_widget moved load from initState to didChangeDependencies first-run guard for context/l10n/locale safety). Per user scope: skipped dead-code earnings_service + pro_bookings_service tmStageLabel; nearby_pro distance wire labels excluded (prior scope); backend DRF messages excluded. `gen-l10n` clean; `analyze` 0 errors (731 info baseline)
- [x] 4.4 Localize remaining standalone helpers shown to users (`lib/app_state.dart` location labels, `lib/utils/` user-facing category strings) via the same triage. (2 new keys: `hmSavedAddresses`, `bfAddress` appended to both ARBs; rest reuse existing keys.) `lib/utils/` audited → **no user-facing display copy** (emergency_categories/category_assets/category_icons = functional match keys + icon/asset lookups; tagalog_service_keywords = functional search data) → excluded as false positive per user. Location labels fixed at the **widget layer** per user (app_state.dart source default args left as-is — no BuildContext there): device-mode branches render `_l10n.hmCurrentDeviceLocation` instead of the English stored default at home `_buildController`, services `_buildLocationPill` + booking-controller build, search_page `_openExpressCheckout`; fallbacks localized (bfPinnedLocation/hmPinnedAddress/svPinnedAddress); services `'All'` category sentinel chip → `_l10n.spAll`; `'Address'` native fallback → `_l10n.bfAddress` in booking_flow_screen/checkout_screen/express_checkout_sheet (both agents added build-local `l10n` where none was in scope) + edit_address `address.addressLine2`; edit_address `'No addresses yet'`/`'Add new address'`/`'Default'` → adNoAddressesYet/adAddNewAddress/adDefault (needed `_l10n` getter added); address_form `'Current device location'`/`'Pinned location'` → hmCurrentDeviceLocation/bfPinnedLocation. Demo data excluded per user (`Metro Manila`, `123 Example Street`, `Home`); latent/dead code (flutter_flow/upload_data, otp_rate_limiter, custom_code) audited, not wired. `gen-l10n` clean; `analyze` 0 errors (732 info baseline)
- [x] 4.5 Re-run the 1.1-style audit across all of `lib/` and confirm zero remaining display literals outside `lib/l10n` (excluding the recorded 1.2 exclude list). **Audit complete via 3 parallel agents + services/auth gap check.** Found and localized remaining live display strings across ~20 files: bookings_model/widget empty states + status labels + error prefix + fallbacks; edit_address 'Select address'; reset_link_sent confirmation; tm_catalog 15 subcategory titles (added `titleKey` + render-site localization in tm_sub_category_screen); tm_active_job_screen TabBar labels, dispatch pills, ETA format, hardware dialog title, provider/vehicle fallbacks; tm_broadcast_screen radius badge, timed out, dispatch mode chips; tm_payment_screen Card/Cash labels + card description (GCash excluded as brand); live_matching 'Route'/'Reference'; product_page 'Contact'/'Book Now'/'Customer'; chat_detail placeholder; search_page quick chips (reuse hmCat*) + 'Booking #' prefix; booking_payment/booking 'Service' fallbacks; checkout 'Card'/'COD'; booking_success calendar event title/details; my_notifications 'Just now' (reuse msJustNow); dispatch/tm_repo fallbacks mapped at render site. ~65 new keys appended to both ARBs. Exclusions honored: onboarding marketing copy, legal/FAQ/chatbot body paragraphs (3.16/3.17 decisions), AI composer field labels, project_create dropdown (3.15 English loanwords), brands (GCash/Visa/Mastercard/Maya), currency/format templates, dead code (uploading, categoriesgrid, password_validation_item, face_verification_auth_flow, geographic_selection_model, booking_models), tm_controller backstops. `gen-l10n` clean; `analyze` 0 errors (737 info baseline).

## 5. Verification & Finalization

- [ ] 5.1 Confirm `app_en.arb` and `app_fil.arb` define the exact same key set (parity check), then run `flutter gen-l10n`
- [ ] 5.2 Run `flutter analyze` and confirm 0 errors (stay within the accepted warnings/notes baseline, no new errors)
- [ ] 5.3 Build a debug APK and smoke-test the `fil` locale on representative screens: Settings, Explore/Home, a booking-flow screen, a profile screen, and one toast/dialog path (e.g. log out or a form error)
- [ ] 5.4 Update `docs/ui-ux-reconstruction-plan.md` status note if the localization work belongs on the current feature branch record, per AGENTS.md conventions
- [ ] 5.5 Final review of the diff (confirming only display literals and ARB/regenerated l10n files changed, no routing/API/behavior edits)
